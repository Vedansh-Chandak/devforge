# 2026-08-03

## DF-016B — Autonomous Coding Layer: TESTS GREEN

Fully implemented DF-016B (autonomous coding on top of deterministic Executor) in `packages/execution/src/executor/`:
`patch-model.ts`, `coding-errors.ts`, `coding-model.ts`, `reasoning-model.ts`, `patch-validator.ts`, `patch-engine.ts`, `diagnostics.ts`, `coding-events.ts`, `repair.ts`. Extended `executor/types.ts`, `executor/report.ts`, `executor/index.ts`.

### Bugs fixed this session (8 failing tests → 0)
1. **Getter snapshot on destructure**: `scriptedCodingModel({ get calls() })` snapshotted `calls` at destructure time. Changed interface to `getCalls: () => number` (also `scriptedReasoningModel` → `getAnalyzeCalls`/`getDecideCalls`). Tests updated to call `getCalls()`.
2. **`countingPatchEngine`**: `Object.assign(wrapper, { get calls() })` copies the *value* (0), not the getter. Switched to `Object.defineProperty(wrapper, 'calls', { get: () => calls })` with a type cast for the intersection return type.
3. **DefaultPatchEngine name**: test expects `name` to contain `default-patch-engine`; previously it used model name verbatim. Now `default-patch-engine(${model.name})` or bare `default-patch-engine`.
4. **Diagnostics test-failure parsing**: `^(?:FAIL|×|✕)` matched BOTH the `FAIL foo > bar` header AND the `× Expected 1 but got 2` line → 2 diagnostics, expected 1. Changed regex to `^[×✕]\s+(.+)$` so only the failure-message line is a diagnostic.
5. **Diagnostics summary counts**: after fix #4 there were 2 compiler + 1 test = 3 errors, but test expects `'2 errors'`. `buildSummary` now skips `TEST`-category diagnostics when counting errors/warnings (test failures reported via their own diagnostics).
6. **Coding-model deep-copy test**: passed `[PATCHES_A]` but called `generatePatch` twice → model "exhausted". Test fixture changed to `[PATCHES_A, PATCHES_A]` (conflicted with the legit "throws when exhausted" test).
7. **repair.ts budget errors leaking as throws**: `run()` now wraps initial `applyAndVerify` AND `runRepairLoop` in try/catch; converts `RepairBudgetExceededError` and `ReasoningError` into `BUDGET_EXCEEDED` reports (with last-known diagnostics) instead of propagating. This fixed "rolls back transaction", "exceeds patch generation budget", "exceeds verification run budget".

### Verification
- `pnpm --filter @devforge/execution check-types` ✓
- `pnpm --filter @devforge/execution build` ✓
- `pnpm --filter @devforge/execution test` → 590 passed (20 files)
- Repo-wide `pnpm check-types` (18 tasks) ✓, `pnpm build` (18 tasks) ✓, `pnpm test` (31 tasks) ✓

Next up (not started): repo-wide lint if configured; DF-016B is otherwise complete per scope.

## DF-016C — Provider-backed Models: TESTS GREEN

Implemented provider-backed CodingModel/ReasoningModel on top of `@devforge/model-provider`, in `packages/execution/src/models/`:
`types.ts`, `errors.ts`, `prompt-builder.ts`, `patch-parser.ts`, `reasoning-parser.ts`, `provider-models.ts`, `index.ts`, plus `tests/` (3 files). Wired into `src/index.ts`.

### What was added (mid-implementation WIP on disk, uncommitted)
The `models/` dir already had types/errors/prompt-builder/patch-parser drafted but UNCOMMITTED and breaking check-types. Completed missing pieces: `reasoning-parser.ts` (parse FailureAnalysis + RepairDecision from tagged JSON), `provider-models.ts` (ProviderCodingModel, ProviderReasoningModel translating provider/parse failures into CodingModelError/ReasoningError).

### Bugs fixed
1. `@devforge/model-provider` was NOT a dependency of execution — added via `package.json` (`workspace:*`), matching planner.
2. `models/types.ts` imported `FailureAnalysis`/`RepairDecision`/`Diagnostics` from wrong modules — `FailureAnalysis`/`RepairDecision` come from `executor/reasoning-model.js`, `Diagnostics` from `executor/diagnostics.js`, `DiagnosticCategory`/`CodePatch` from `executor/patch-model.js`.
3. `models/errors.ts` passed `{...options, code}` into an options type that only had `cause` — introduced `IntegrationErrorOptions` (code+cause). ParseError now takes a dedicated `ParseFailureOptions`.
4. `models/patch-parser.ts`: `&&`/`??` mixing + `bestCandidate?.length ?? 0` + `operation` cast to `CodePatchOperation`.
5. `prompt-builder.ts` import fixes matching real module exports.
6. `noUncheckedIndexedAccess`: test array accesses need `!`.

### Tests (52 new)
patch-parser (12), reasoning-parser (17), prompt-builder (9), provider-models (14). provider-models reuses FakeModelProvider from model-provider; the "decides repairs" test uses a tiny cycling provider because FakeModelProvider returns the same response every call.

### Verification
- `pnpm --filter @devforge/execution check-types` ✓, `build` ✓
- execution test → 642 passed (24 files), including 52 new model tests
- Repo-wide `pnpm check-types` (18 tasks) ✓, `pnpm build` (18 tasks) ✓, `pnpm test` (31 tasks) ✓, `pnpm lint` (3 configured) ✓

DF-016C complete and green.
